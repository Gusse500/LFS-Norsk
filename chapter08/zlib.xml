<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-zlib" role="wrap">
  <?dbhtml filename="zlib.html"?>

  <sect1info condition="script">
    <productname>zlib</productname>
    <productnumber>&zlib-version;</productnumber>
    <address>&zlib-url;</address>
  </sect1info>

  <title>Zlib-&zlib-version;</title>

  <indexterm zone="ch-system-zlib">
    <primary sortas="a-Zlib">Zlib</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Zlib pakken inneholder komprimerings- og dekompresjonsrutiner som brukes av
    noen programmer.</para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&zlib-fin-sbu;</seg>
        <seg>&zlib-fin-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Installasjon av Zlib</title>

    <para>Forbered Zlib for kompilering:</para>

<screen><userinput remap="configure">./configure --prefix=/usr</userinput></screen>

    <para>Kompiler pakken:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>For å teste resultatene, utsted:</para>

<screen><userinput remap="test">make check</userinput></screen>

    <para>Installer pakken:</para>

<screen><userinput remap="install">make install</userinput></screen>

    <para>Fjern et ubrukelig statisk bibliotek:</para>

<screen><userinput remap="install">rm -fv /usr/lib/libz.a</userinput></screen>

  </sect2>

  <!-- - - - - - - - - - -->
  <!-- Multilib - 32bit  -->
  <!-- - - - - - - - - - -->

  <sect2 arch="ml_32,ml_all" role="installation">
    <title>Installasjon av Zlib - 32bit</title>

    <para>Rens forrige bygg:</para>

<screen><userinput remap="pre">make distclean</userinput></screen>

    <para>Klargjør Zlib for kompilering:</para>

<screen><userinput remap="configure">CFLAGS+=" -m32" CXXFLAGS+=" -m32" \
./configure --prefix=/usr \
    --libdir=/usr/lib32</userinput></screen>

    <para>Kompiler pakken:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>Installer pakken:</para>

<screen><userinput remap="install">make DESTDIR=$PWD/DESTDIR install
cp -Rv DESTDIR/usr/lib32/* /usr/lib32
rm -rf DESTDIR</userinput></screen>

  </sect2><!-- m32 -->

  <!-- - - - - - - - - - -->
  <!-- Multilib - x32bit -->
  <!-- - - - - - - - - - -->

  <sect2 arch="ml_x32,ml_all" role="installation">
    <title>Installasjon av Zlib - x32bit</title>

<screen><userinput remap="pre">make distclean</userinput></screen>

    <para>Klargjør Zlib for kompilering:</para>

<screen><userinput remap="configure">CFLAGS+=" -mx32" CXXFLAGS+=" -mx32" \
./configure --prefix=/usr    \
    --libdir=/usr/libx32</userinput></screen>

    <para>Kompiler pakken:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>Installer pakken:</para>

<screen><userinput remap="install">make DESTDIR=$PWD/DESTDIR install
cp -Rv DESTDIR/usr/libx32/* /usr/libx32
rm -rf DESTDIR</userinput></screen>

  </sect2><!-- mx32 -->
  <sect2 id="contents-zlib" role="content">
    <title>Innhold i Zlib</title>

    <segmentedlist>
      <segtitle>Installerte biblioteker</segtitle>

      <seglistitem>
        <seg>libz.so</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Korte beskrivelser</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="libz">
        <term><filename class="libraryfile">libz</filename></term>
        <listitem>
          <para>Inneholder komprimerings- og dekompresjonsfunksjoner som brukes av
          noen programmer</para>
          <indexterm zone="ch-system-zlib libz">
            <primary sortas="c-libz">libz</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
